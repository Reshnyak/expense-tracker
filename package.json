{
  "name": "expence-tracker",
  "version": "0.0.0",
  "private": true,
  "description": "Shared expense tracker — monorepo (React SPA + Go API)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "openapi:gen": "pnpm --filter web openapi:gen"
  }
}
