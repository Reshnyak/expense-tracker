// Package service contains the business logic (spaces, expenses, balances,
// auth/session handling). It orchestrates the generated sqlc queries and is
// consumed by internal/httpapi/handlers.
package service
